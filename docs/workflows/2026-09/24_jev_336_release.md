# Jev Token 统计 .336 发布与主站滚动更新

## 范围

用户授权合并并上线 Jev。主程序 PR #270 与插件 PR #7 已合并，
发布版本 v1.0.0-rc.10.1.10.336，插件 cloudflare-jev@0.2.3。
只操作 CloudSSH API中转站 serverId=38、hostId=11 的 maolaoapi 三节点，
顺序 maolaoapi → maolaoapi-slave-1 → maolaoapi-slave-2；不更新其他站点。

## 执行计划

预检已确认三节点 .335、健康，活动插件为0.2.2，渠道别名映射正确。
先保留Compose/环境/模块与数据库备份，验证固定发布镜像revision和摘要。
使用既有管理API安装并激活经过hash核验的新插件，再逐节点重建应用并验证版本、
健康、运行配置和插件路由；PostgreSQL与Redis不重启，站点价格不变。
现有模型请求503另按请求ID查证，不能把统计修复当作已解决所有供应商异常。

## 验证与回退

重基后五组Go回归、插件宿主完整用量链路及CI通过。
插件源码SHA256为 fbb6beadfea0f4d6573abdb654ba61ae22a03465f004111b535da5b5ca8da311。
失败节点停止后续更新，按备份恢复旧.335镜像；插件可切回0.2.2。
不自动覆盖数据库、不补造历史Token日志；实际费用与输出免费配置保持。

## 实际结果

发布与三节点升级已完成，详细证据和验收边界如下。

## 已完成发布

主程序 #270 合并提交 `5255a0dcb761d9b8a12e118c381f368dde1ed924`，
插件 #7 合并提交 `ad210868a2fa95b070b63bf9d46deb4b0afef639`。
发布准备 #272 合并后，`.336` 标签固定于 `9dfdafce8a981047c8cd1623ca0ae9722b9f7e7f`。
Linux工作流 `35970381141`、多架构镜像工作流 `35970381076` 均成功。
镜像固定摘要为 `sha256:202011ca05dcc7879d17c449306a120b0d1a3b8047d50e28e6e4499be6a71db5`。

## 备份与插件安装

备份作业 `49e0130c-22f3-4653-be78-9ee913c0af05` 成功，退出码0。
目录 `/home/docker/maolaoapi/backups/pre-336-20260924T073354Z`。
PostgreSQL归档 2,162,830,566 字节，SHA256
`f87a0681c96420d06bf9c17ccc4e319e24e56da6197a24008b2ab44a3129a6c0`，
`pg_restore --list` 通过，未进行完整恢复演练。Compose、环境文件和29个模块文件已备份。

通过站点管理API安装插件并在三个节点分别激活0.2.3，作业
`9ac896fc-01a5-4446-8546-7386ec332de6` 成功。安装前核对原始字节SHA256与已合并索引；
新旧版本并存，旧0.2.2未删除。没有修改渠道映射、售价或用户Token。

## 三节点更新结果

按以下顺序完成，每个作业成功、退出码0、healthy、restart=0：

- `maolaoapi`：`1ee19d3b-82a3-4bdf-ac20-4646899f1572`，18095。
- `maolaoapi-slave-1`：`7d2d29b5-5727-474f-ad9b-5bdaff6e9c88`，18100。
- `maolaoapi-slave-2`：`d8553c03-3146-479a-ba7d-63c67c830a07`，18101。

三节点实际进程和镜像二进制均为 `.336`，SHA256
`9610db24e4b404c51708f52765466aa16c36baba878ecb98325f285a0702aaaa`。
环境、挂载、端口、启动参数、模型校验配置与245条历史记录保持；
PostgreSQL、Redis身份/启动时间/重启计数保持。没有更新zzapi或其他应用。

最终验证作业 `6625c213-32c5-4e5a-b372-c83871ecba45` 成功：
三节点插件均0.2.3、active/enabled且hash一致；模块29个文件hash保持。
远端访问公网被Cloudflare返回403，本机独立请求公网 `/api/status` 为200、success=true、版本`.336`。
三节点受保护管理入口匿名401，Classic页面200。

## 截图503与验证范围

截图请求ID定位到从节点2，应用日志为503、处理时间5.943751ms，证明页面5.86秒并非上游推理耗时。
没有足够历史诊断字段将其唯一归因于某个门禁，因此不宣称统计代码直接修复所有503。
刷新并升级后，使用已有测试令牌向三节点发送无效空JSON，均由当前插件decoder返回预期400；
作业 `62e87071-c5dc-463f-80da-f7067e4aeaa8` 成功，未进入供应商或触发收费。

本次上线确认宿主、插件、认证与原生路由；真实供应商成功请求及线上Token落库仍待用户后续测试。
完整486/70用量与按次/表达式金额已由本地模拟上游集成验证，不能当作本次真实收费验收。
回退脚本位于 `/home/docker/maolaoapi/releases/336/deploy.py`，`rollback <节点名>` 恢复`.335`，
不自动恢复数据库；插件可重新激活0.2.2。本轮未执行回退。
