# 本地待交付修复 .338 整合与 maolaoapi 发布

## 目标和范围

用户要求将此前列出的本地待交付项全部整合到主分支，连同充值日志不记录 IP 修复
发版并逐个更新 maolaoapi。本轮不更新 zzapi，不直接提交旧工作区残留和 stash。

- Classic 零输出价格显示与同模型配置回填。
- Classic 损坏登录缓存恢复与路由守卫校验。
- xAI 流式缓存用量跨块合并、显式零值与 Claude 中断用量归一化。
- Claude 思考等级日志恢复，保持协议转换和账务输入边界。
- 管理员后台限流豁免，保留真实鉴权、所有权及个人安全流程保护。
- 任务插件通用测试明确返回不支持，Jev 使用 `/v1/systemone`。
- 补充 maolaoapi `.335` 部署历史记录。
- PR #277 的充值业务日志不记录、不返回 IP 已单独合并。

## 整合方法与兼容

从最新 `custom-main` 创建隔离工作区，保留原工作区未提交数据。PR #259 已包含 xAI
两项原提交的等价实现；本轮集成其增量及文档。PR #262 的登录缓存部分与本地缓存
恢复相同，其其他密码限流与资源修复不属于截图待交付范围。
管理员草稿复用主分支现有取密钥限流配置，移除重复定义，保留后续安全更新。
文档索引追加冲突逐段合并，不用旧文件覆盖主分支。

前端修复仅涉及 Classic；日志、限流、任务插件接口由两套模板共用。
不新增数据库表或字段；不修改在线渠道、价格、插件激活版本、令牌或用户余额。

## 发布与部署计划

固定标签 `v1.0.0-rc.10.1.10.338`，等待 Linux Release 与 AMD64/ARM64 镜像产物成功。
CloudSSH「API中转站」，serverId=38、hostId=11；部署目录 `/home/docker/maolaoapi`。
预检三应用均为 `.336`、healthy、restart=0，端口为 18095、18100、18101。

先备份 PostgreSQL、Compose、环境及模块，保留运行参数摘要；验证数据库归档可读。
校验固定镜像 digest、revision 与运行二进制。只修改三个应用的镜像引用，结构化比较
其他配置保持。严格按 maolaoapi → maolaoapi-slave-1 → maolaoapi-slave-2 顺序重建，
每个节点健康、版本、权限门禁、实际进程二进制与配置通过后继续。
数据库与 Redis 不重启，不发起收费模型调用。失败节点停止后续部署，按备份恢复该
节点 `.336` 镜像，不自动恢复在线数据库。

## 验证与实际结果

待最终集成验证、发布及逐节点执行后记录。
